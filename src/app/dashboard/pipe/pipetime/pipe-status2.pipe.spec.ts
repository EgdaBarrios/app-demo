import { PipeStatus2Pipe } from './pipe-status2.pipe';

describe('PipeStatus2Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipeStatus2Pipe();
    expect(pipe).toBeTruthy();
  });
});
