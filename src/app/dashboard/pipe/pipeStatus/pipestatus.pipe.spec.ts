import { PipestatusPipe } from './pipestatus.pipe';

describe('PipestatusPipe', () => {
  it('create an instance', () => {
    const pipe = new PipestatusPipe();
    expect(pipe).toBeTruthy();
  });
});
